<div class="commandes-list">
  <div class="page-header">
    <h1>Mes commandes</h1>
    <%= link_to "Nouvelle commande", new_commande_path, class: "btn btn-primary" %>
  </div>

  <% if @commandes.any? %>
    <div class="commandes-grid">
      <% @commandes.each do |commande| %>
        <div class="commande-card">
          <div class="card-header">
            <h3>Commande #<%= commande.id %></h3>
            <div class="status-badge status-<%= commande.statut %>">
              <%= commande.statut.humanize %>
            </div>
          </div>
          
          <div class="card-body">
            <div class="commande-info">
              <div class="info-row">
                <strong>Date:</strong>
                <%= commande.created_at.strftime('%d/%m/%Y à %H:%M') %>
              </div>
              <div class="info-row">
                <strong>Retrait prévu:</strong>
                <%= commande.formatted_pickup_time %>
              </div>
              <div class="info-row">
                <strong>Articles:</strong>
                <%= commande.total_items %> plat<%= commande.total_items > 1 ? 's' : '' %>
              </div>
              <div class="info-row total">
                <strong>Total: <%= commande.montant_total.to_f %>€</strong>
              </div>
            </div>
            
            <div class="items-preview">
              <% commande.order_items.limit(3).each do |item| %>
                <div class="item-preview">
                  <%= item.plat.nom %> (x<%= item.quantite %>)
                </div>
              <% end %>
              <% if commande.order_items.count > 3 %>
                <div class="item-preview more">
                  ... et <%= commande.order_items.count - 3 %> autre<%= commande.order_items.count - 3 > 1 ? 's' : '' %>
                </div>
              <% end %>
            </div>
          </div>
          
          <div class="card-actions">
            <%= link_to "Voir détails", commande, class: "btn btn-outline-primary" %>
            <% if commande.can_be_cancelled? %>
              <%= link_to "Annuler", commande, method: :delete, 
                          confirm: "Êtes-vous sûr de vouloir annuler cette commande ?", 
                          class: "btn btn-outline-danger btn-sm" %>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="empty-state">
      <div class="empty-icon">📋</div>
      <h3>Aucune commande</h3>
      <p>Vous n'avez pas encore passé de commande.</p>
      <%= link_to "Passer ma première commande", new_commande_path, class: "btn btn-primary btn-lg" %>
    </div>
  <% end %>
</div>

<style>
.commandes-list {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30px;
  padding-bottom: 20px;
  border-bottom: 2px solid #d4572a;
}

.page-header h1 {
  margin: 0;
  color: #333;
}

.commandes-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
  gap: 20px;
}

.commande-card {
  background: white;
  border-radius: 12px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  overflow: hidden;
  transition: transform 0.2s, box-shadow 0.2s;
}

.commande-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 15px rgba(0,0,0,0.15);
}

.card-header {
  background: linear-gradient(135deg, #d4572a, #e67e22);
  color: white;
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.card-header h3 {
  margin: 0;
  font-size: 18px;
}

.status-badge {
  padding: 6px 12px;
  border-radius: 15px;
  font-size: 12px;
  font-weight: bold;
  text-transform: uppercase;
  background: rgba(255,255,255,0.2);
  backdrop-filter: blur(10px);
}

.card-body {
  padding: 20px;
}

.commande-info {
  margin-bottom: 20px;
}

.info-row {
  display: flex;
  justify-content: space-between;
  margin-bottom: 8px;
  color: #666;
}

.info-row strong {
  color: #333;
}

.info-row.total {
  font-size: 16px;
  color: #d4572a;
  font-weight: bold;
  padding-top: 10px;
  border-top: 1px solid #eee;
  margin-top: 15px;
}

.items-preview {
  background: #f8f9fa;
  padding: 15px;
  border-radius: 8px;
  margin-bottom: 20px;
}

.item-preview {
  font-size: 14px;
  color: #666;
  margin-bottom: 5px;
}

.item-preview:last-child {
  margin-bottom: 0;
}

.item-preview.more {
  font-style: italic;
  color: #999;
}

.card-actions {
  padding: 0 20px 20px;
  display: flex;
  gap: 10px;
  justify-content: space-between;
}

.btn {
  padding: 10px 20px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  text-decoration: none;
  display: inline-block;
  text-align: center;
  transition: all 0.2s;
  font-weight: 500;
  border: 2px solid transparent;
}

.btn-primary {
  background-color: #d4572a;
  color: white;
}

.btn-primary:hover {
  background-color: #b8481f;
  transform: translateY(-1px);
}

.btn-outline-primary {
  background-color: transparent;
  color: #d4572a;
  border-color: #d4572a;
}

.btn-outline-primary:hover {
  background-color: #d4572a;
  color: white;
}

.btn-outline-danger {
  background-color: transparent;
  color: #dc3545;
  border-color: #dc3545;
}

.btn-outline-danger:hover {
  background-color: #dc3545;
  color: white;
}

.btn-lg {
  padding: 15px 30px;
  font-size: 16px;
}

.btn-sm {
  padding: 8px 16px;
  font-size: 12px;
}

.empty-state {
  text-align: center;
  padding: 60px 20px;
  color: #666;
}

.empty-icon {
  font-size: 48px;
  margin-bottom: 20px;
}

.empty-state h3 {
  color: #333;
  margin-bottom: 10px;
}

.empty-state p {
  margin-bottom: 30px;
  font-size: 16px;
}

/* Status badge specific styles */
.status-en_attente {
  background-color: rgba(255, 243, 205, 0.9);
  color: #856404;
}

.status-confirmee {
  background-color: rgba(212, 237, 218, 0.9);
  color: #155724;
}

.status-prete {
  background-color: rgba(209, 236, 241, 0.9);
  color: #0c5460;
}

.status-recuperee {
  background-color: rgba(226, 227, 229, 0.9);
  color: #495057;
}

.status-annulee {
  background-color: rgba(248, 215, 218, 0.9);
  color: #721c24;
}

@media (max-width: 768px) {
  .page-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 20px;
  }
  
  .commandes-grid {
    grid-template-columns: 1fr;
  }
  
  .card-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }
  
  .card-actions {
    flex-direction: column;
  }
}
</style> 